# Changelog

## [Unreleased]

### Features
- **macOS — Accessibility API text insertion:** New **Accessibility API** option (Settings → Text insertion) sets `AXSelectedText` on the focused element when Accessibility is trusted; **Automatic** on macOS uses that path first, then **Paste** if the target does not support it. **Config v10** is a schema bump for the new method; existing installs migrate on load.
- **Settings → Text insertion:** Dedicated tab for default injection method (Auto / keystrokes / clipboard), Auto threshold, keystroke settle time, retry settings, and **per-app rules** with the same installed-app picker as Privacy. **Config v9** migrates legacy `force_clipboard_apps` into `app_injection_rules`; matching is cross-platform (foreground process name substring). Fresh defaults still include a Notepad → clipboard rule.
- **Dormant pill is now oval-shaped** (48px × 28px) instead of circular, keeping the color-coded accent visible but more compact.
- **Per-mode accent color (config + UI):** Each `DictationMode` stores `accent_color` (any CSS color string, usually OKLCH). **Dictation** edit panel: preset swatches + custom field. List rows and status bar use the stored color; overlay dormant dot receives it via `overlay-state`. **Config v7** migration fills defaults for existing installs. Recipe **import/export** includes optional `accent_color`.
- **Color-coded dictation modes** in the main app: StatusBar mode selector shows each mode’s configured accent (not a fixed emoji map). Mode dropdown uses the same per-mode colors.
- **Multi-PC sync (Phase 9, Pro):** Website: D1 `sync_items`, `POST/GET /api/sync/push|pull`, `POST /api/sync/reset` (Bearer license key, last-write-wins). Desktop: pull/merge/push for notes/tasks/snippets, dictionary, modes, synced settings subset, and **encrypted** `provider_keys` (HKDF + AES-256-GCM); IPC `sync_now`, `get_sync_status`, `enable_sync`, `disable_sync`, `reset_sync`; startup + 5-minute sync; **Settings → Multi-PC sync** (license key, toggle, Sync now, reset server). API base = `recipe_library_url`; eligibility via `GET /api/license/validate`.
- **Community recipe library (Phase 8):** Config `recipe_library_url` (default `https://kalam.stream`); recipe file **v2** adds optional `description`, `category`, `tags` (v1 still imports); **Browse recipes** panel on Dictation (search, categories, install); IPC `fetch_recipe_library`, `install_recipe_from_library`; **`kalam://import-recipe?slug=…`** deep link + single-instance argv handling; **Share to library** opens the website submit page. Sister **kalam-website**: D1 `recipes` table, public `/api/recipes` CRUD + download, marketing `/recipes`, `/recipes/[slug]`, authenticated `/recipes/submit`, admin **Recipes** screen.
- **Auto-activation rules (Phase 7):** Per-mode regex rules on process name, window title, or bundle id; background watcher switches **active mode** when the foreground app matches, then **restores** the previous mode when focus leaves (manual mode change clears tracking). **Dictation** page rule editor (test against running apps, pick process to fill a literal pattern). Overlay toast on switch/restore; event **`auto-activate-switched`**. Planned **Pro** gating deferred until license/PlanStatus ships.
- **Overlay (Phase 6):** Three layouts — **Dormant** idle pill (mode + mic, hover actions, right-click menu), **Mini** compact active bar (waveform, context icons, expand / stop), **Full** panel (polish toggle, on-demand context previews, pipeline stepper, mode menu). Settings: **Active overlay style** (Mini/Full) and **Always show overlay**. New IPC: `resize_overlay_to`, `copy_last_transcription`, `toggle_polish_from_overlay`, `get_context_previews`; `overlay-state` uses **`Dormant`** instead of `Collapsed`.
- **Voice-activated editing (Phase 5):** Optional **Voice editing** hotkey (Settings → Dictation Hotkeys). Hold to capture highlighted text (Windows Ctrl+C shim), speak an instruction, then replace the selection via the **active mode’s LLM** and clipboard paste. `RecordingType::VoiceEdit` pipeline with dedicated system prompt; blocked on sensitive apps and on non-Windows until selection capture exists. Overlay + status bar show Editing / Transforming; history `session_mode` **`voice_edit`**.
- **Context awareness (Phase 4):** Windows UI Automation reads focused/active-window text (with timeout); clipboard + date/time/username; selected text via Ctrl+C at dictation start. Honors global and per-mode toggles; sensitive apps skip all context and skip LLM when the only reason to call the model was context. Overlay shows context hint while recording/processing; Dictation page exposes full context settings.
- **Curated model library (Phase 2):** Rust `catalog` with recommended STT/LLM providers (Groq, OpenAI, Anthropic, Gemini, OpenRouter, local); `get_model_catalog`, `set_provider_key`, `remove_provider_key`, `test_provider_key`; unified **`provider_keys`** in config (v3 migration from nested STT/command maps); **Settings → Provider library** cards + **Re-run setup wizard**; **Custom (OpenAI-compatible URL)** default LLM option with `custom_openai_endpoint`; **Dictation** mode editor lists providers that have saved keys (catalog-driven); onboarding writes keys into `provider_keys`.
- **Dictation modes (Phase 1):** config v2 with built-in and custom modes, global **Polish** toggle, mode-cycle hotkey, **Dictation** page (editor, recipe import/export placeholder browse), status bar mode switcher, overlay active-mode label; default STT + LLM moved to **Settings → AI providers**; onboarding **setup path** (offline / Groq one-key / best quality / manual) and **Polish** toggle on the try-it step.
- **Dictionary** is a main sidebar page (above Snippets) with a **single list**: hint-only rows (transcription vocabulary) and **find → replace** rows; **New item** opens the slide-over panel (empty “Replace with” = vocabulary-only). Tray menu includes Dictionary.
- **Replacements:** dictionary UI adds **plain-text** find/replace (case-insensitive). Legacy or hand-edited `custom_rules` may still use regex in the formatter.
- **Cloud STT vocabulary prompt:** enabled **literal** replacement targets (non-empty, ≤80 chars, no newlines) are merged into the Whisper `prompt` with dictionary terms (deduped), so the model is nudged toward the desired wording; replacements still run after transcription as a fallback. Regex rules do not contribute to the prompt.

### Changes
- **Overlay pill design polish:** Comprehensive visual refresh of the floating pill. Dormant state: higher base opacity (0.85), larger 10px swatch with ambient accent glow, slower 4.5s beacon breath, magnetic pre-hover response when cursor enters hit-test zone. Easing curves: replaced bouncy/elastic springs with refined expo/quart deceleration for decisive, premium motion. Button presses: gentler scale(0.93–0.95) + translateY(1px) instead of aggressive scale(0.85). Hover-expand choreography: orchestrated stagger — shell opens first, then left content slides in (100ms delay), then right icons (160ms delay). Recording glow: 50% border opacity, 10px box-shadow, faster 1.2s rec-pulse. Typography: mode name weight 600 with letter-spacing, hotkey styled as recessed kbd badge. Success state: bolder 55% success border, 14px glow, accent-tinted icon wrap with radiating box-shadow. Spinner: replaced morphing blob with clean ring spinner (GPU-friendly, `border-top-color` technique). Hello animation: confident scale+translate entrance on first appearance (600ms expo). Clock: expanded dormant row shows current time (tabular-nums, 30s refresh). Microcopy: "Still working…", "Trying again… (N)", "No transcription yet", "Hold longer to dictate". Reduced-motion: all new animations and transitions disabled.

### Fixes
- **Overlay pill unreliable hover-expand and dropdown collapse:** Multiple interacting issues caused the pill to (a) require several hover attempts before expanding, (b) collapse while the mode dropdown was open, and (c) clip the dropdown after re-expand. Fixes: `onBlipMouseEnter` now arms the idle-retract guard *before* the resize IPC so the synthetic mouseleave from the HWND reposition is absorbed; `retractDormantChromeToDotIfIdle`, `onBlipMouseLeave`, the pointer-move retract guard, and the new `cursor-left-pill` listener all bail out when a menu is open; the reactive menu-open block arms the retract guard on close so the HWND shrink doesn't trigger a bogus collapse; mode menu toggle (`toggleModeMenu`) awaits the `set_overlay_menu_open` IPC before rendering the dropdown so the HWND is canvas-sized before the first paint; wired up the Rust `cursor-left-pill` event (previously emitted but never listened to) with retract-guard awareness; hit-test grace bumped from 200ms to 350ms to cover CSS transitions.
- **Overlay mode dropdown / context menu clipped:** The overlay window is tight to the pill; DOM menus were drawn outside the WebView client area and were cut off. `set_overlay_menu_open` now expands the HWND to the tier canvas while a menu is open and restores the tight size when it closes; same-tier resizes also reposition when size changes so **Expand up** keeps the bottom edge anchored.
- **Overlay pill bounce on hover/leave:** The pill visually jumped up then down when hovering or leaving because `resize_overlay` called `set_size` before `set_position` (OS keeps top-left fixed, so the anchor edge shifted until repositioned). Both `resize_overlay` and `resize_overlay_tight_fit` now set position **before** size, and tight-fit respects `expand_direction` for the Y delta instead of always centering.
- **Overlay hover flicker:** Dormant pill could **flicker, jump position, and become unclickable** when hovering. Three interacting causes: (1) `resize_overlay` expanded the window to the full tier canvas then repositioned, visible as the pill jumping up then expanding width; (2) the 200ms cursor-tracking loop repositioned the window mid-transition, fighting with tight-fit; (3) `reportPillDimensionsNow` skipped when hovered so the window stayed oversized until leave, causing a second resize flash. Fixed by: tight-fit now runs during hover (with retract guard to absorb synthetic mouseleave), both `resize_overlay` and `resize_overlay_tight_fit` suppress the position-tracking loop briefly, `onBlipMouseEnter` arms the retract guard before the resize IPC, and hit-testing uses the full client rect when the window is tight-fit. Previously, the pointer looked “on” the pill while Windows treated it as outside → **click-through** toggled and Svelte fired repeated enter/leave. This is also fixed by the changes above.
- **Overlay (Windows/Linux):** Transparent margins around the pill could receive right-clicks and show the native window menu (minimize / maximize / close). The overlay HWND is now **tight-fit** to the pill after layout (plus a small shadow gutter), **hit-test/positioning** use the real inner size, **Windows** subclasses the HWND to ignore **`WM_CONTEXTMENU`**, **macOS** tweaks the overlay `NSWindow` for passive utility behavior, and **Linux** sets **minimizable/maximizable/closable** off on the overlay window in config.
- **Toggle dictation + focus switch:** After switching to another app while recording, transcribed text could paste or type into the wrong window. Windows now restores the dictation-start window with **Alt + `AttachThreadInput` + `SetForegroundWindow`** (bare `SetForegroundWindow` often failed from a background process). **macOS** saves the foreground PID and runs **osascript** (`System Events`) before inject; **Linux (X11)** saves the X11 window id and runs **`xdotool windowactivate`** when available (best-effort on Wayland if `xdotool` is missing or ineffective).
- **Text insertion (simulate keystrokes):** Typing speed (ms per character) now waits **between** each simulated key when delay is greater than 0, matching Settings; delay 0 still sends text in one fast batch. Previously the app slept once after a single `enigo.text` call, so the control had little effect during typing.
- **Sensitive apps picker:** installed-app scan runs on a **blocking thread pool** (async command) so the UI stays usable while scanning; closing the panel **clears loading immediately** and **ignores stale** scan results. **Duplicate rows** (same display name, e.g. many Firefox helpers) are **merged** server-side with a simple priority heuristic.
- **Email mode context was off by default** despite the upgrade plan specifying it should ship with context enabled; `build_default_modes` and `recipes/email.json` now set `context.enabled: true`.
- **Polish not active for new users:** `polish_enabled` stayed `false` until manually toggled. Onboarding now defaults polish on for AI-capable setup paths (one-key, best-quality, manual) and off for offline.
- **Mode tile click immediately activated the mode** instead of just selecting it for editing. Single click now selects for editing (dashed outline); double-click activates the dictation mode.
- **Overlay context previews ignored global toggle:** `get_context_previews` read foreground app and clipboard unconditionally. Now returns empty when `context_awareness_enabled` is off or a sensitive app is focused.
- **TS `AppConfig.modes` was optional** despite Rust always populating it after migration. Made `modes`, `active_mode_id`, `polish_enabled`, `polish_config`, `context_awareness_enabled`, `mode_cycle_hotkey`, `voice_edit_hotkey`, and `recipe_library_url` non-optional in `types.ts`; removed dead `defaultPolishConfig()` and `?? []` fallbacks.
- **STT prompt leakage at end of transcription:** `sanitize_prompt_leakage` only stripped dictionary/vocabulary terms echoed at the **start** of Whisper output; Groq can also append them at the **end**. Added trailing exact-suffix stripping and trailing comma-separated vocab term stripping (with punctuation tolerance) so leaked dictionary words no longer appear in the final transcription.
- **Dictionary:** list and empty state use the same horizontal width as the page header (removed inner `max-width` that made the header wider than the list).
- **Dictionary:** clearing “Replace with” on a replacement row now correctly converts the entry to vocabulary-only (removed faulty `editHasReplacement` gate). Vocabulary → replacement no longer closes the panel before save finishes. Dictionary SQLite errors and duplicate terms (case-insensitive) surface in the UI; legacy **regex** rules are validated on save via `validate_replacement_pattern` (same flags as the formatter). Replacement rows with `enabled: false` show an **Inactive** hint in the list.
- Apply rustfmt in `src-tauri` so `cargo fmt --check` passes in the release workflow; fix Clippy (`manual_pattern_char_comparison` in STT vocab split, `needless_return` in Windows permission status) so `cargo clippy -- -D warnings` passes in CI.

### Changes
- **Dictation → mode editor:** Icon field is a **Choose icon** control that opens a searchable dialog (curated Iconify set) instead of an inline grid. **Accent** presets end with a **custom swatch** (dashed ring) that opens the native color picker; no separate “Custom color” row. Accordion bodies have clearer vertical padding; **Context** toggles use lighter typography than section headers; **Add rule** is spaced below rules with a dashed separator.
- **Settings → Connections → Providers & API keys:** Rebuilt to use the same `setting-row` / `setting-label` / `setting-control` pattern as the rest of Settings. Connected providers show as flat rows (icon + name + capability + masked key) with Replace key / Remove controls. Disconnected providers no longer clutter the list. Removed the Re-run setup wizard button.
- **Diagnostics** moved from the main sidebar and system tray into **Settings → Advanced** (collapsible **Diagnostics** section below **Logging**). The Advanced **Logging** section title no longer says “Diagnostics”. Each diagnostic run temporarily enables in-app logging at **Debug** and restores your previous logging settings afterward. **Save report (.md)** includes structured rows for tests run since the last save and a log excerpt; tray **Run Diagnostics** was removed.
- **Automatic text insertion** is **OS-aware**: on **macOS**, Automatic uses Accessibility API when possible, then clipboard paste; on **Windows/Linux**, behavior is unchanged (short text typed, longer text pasted per threshold). Synced configs may include **Accessibility API** on non-Mac systems — insertion falls back to paste there; Settings shows a clarifying option label when that value is selected.
- **Settings → Text insertion → Application-specific rules:** Replaced the wide grid table with **stacked cards** that reuse the same `setting-row` layout as the defaults block, so labels and controls stay aligned and **narrow windows** stack like the rest of Settings (no broken multi-column grid).
- **Settings → Text insertion:** Defaults only show **length before pasting** and **pause between letters** when they apply to the selected method; each per-app rule shows the same fields only when relevant. Numeric fields **clamp** on load and save. **Pause between letters** is capped at **100 ms** so very large values can’t make dictation impractical (older configs above that reduce on next save or launch). Labels use plainer language (e.g. “How text is added”, “Paste”, “Try again”).
- **Windows keyboard hook:** Unmapped virtual keys (e.g. punctuation while typing) are logged at **debug** instead of **warn**, so the dev console stays readable; enable **Settings → Advanced** in-app logging at **Debug** to capture those lines when diagnosing VK mapping.
- **Default mode rename + indestructibility (config v8):** Built-in "Voice" mode renamed to **"Default"** (`id: "default"`, `name: "Default"`). The default mode cannot be deleted — `is_deletable: false` is enforced on every config load. If someone manually removes it from `config.json`, `ensure_default_mode_exists()` re-creates it automatically on startup. Config migration v7→v8 renames existing "voice" modes; sync merge ignores delete requests for the default mode.
- **AI config architecture refactor (config v6):** Flattened the convoluted AI provider/mode settings into a clear two-layer system. **Command mode** is no longer a standalone dictation mode — it's a cross-cutting capability triggered by hotkey from any active mode. Removed `CommandConfig.provider/models/api_keys` (legacy fields kept for migration only). Added `AppConfig.default_llm_provider` + `default_llm_model` as the global fallback LLM. Eliminated the `"inherit"` provider concept — each mode now stores concrete provider/model values. Config migration v5→v6 resolves all inherit refs, promotes command LLM to global default, and removes the built-in "Command" dictation mode. Settings "AI providers" tab renamed to **Connections** with a new **Default language model** section replacing the old "LLM & command hotkey" section. Dictation page mode editor shows concrete providers instead of "Inherit (Settings)".
- **Overlay pill redesign:** Complete UI overhaul informed by adversarial persona validation (6 user archetypes). Dormant pill shrunk to 240px with click-to-expand tray (replaces fragile hover-to-reveal). Default opacity raised from 72% to 88%. New OKLCH color system replaces cyan-on-dark AI palette with desaturated, tinted-neutral tokens. Pipeline stepper removed; Full panel shows context source names only (not clipboard/selection content) for privacy. Cancel button always visible during processing. Errors persist until dismissed (no 10s auto-dismiss). Plain-language error mapping (`friendlyError`). "Polish" renamed to "Clean up" throughout. "ForceLocal" renamed to "Local only — no cloud, no context". Processing uses CSS spinner instead of bouncing dots. `prefers-reduced-motion` support added. Focus-visible rings on all interactive elements. `role="application"` replaced with `role="status"`. Success auto-dismiss shortened from 3s to 2s. Auto-activate toast shortened and deduped. "Rec" → "Record", "Full" → "Expand panel", "Mini" → "Compact".
- **Settings → Privacy → Sensitive apps:** **Add app** uses a **right slide-over panel** (same pattern as Dictionary): **installed applications only**, list **loads as soon as the panel opens** (removed Running now, Browse file, and the separate “Load installed apps” action).
- **Dictation page — unified mode list:** Replaced the hero card + sunken tray split with a single flat list of equal card rows (matching the Tasks list style). The selected mode is indicated by a left accent bar; click any row to switch. Removed the "Active/Inactive" framing entirely.
- **Config v5:** Phase 9 sync fields (`license_key`, `sync_enabled`, `sync_last_at`, `sync_device_id`, `sync_config_dirty`, `sync_settings_rev`, merge cursors); auto-migration from v4.
- **Config v4:** Phase 6 overlay preferences (`overlay_active_preference`, `overlay_always_visible`); auto-migration from v3.
- **Overlay window sizing** is three-tier (dormant / mini / full logical sizes) instead of collapsed/expanded binary; default overlay webview size in `tauri.conf.json` increased to fit the full panel.
- **Dictionary:** **New item** in the header opens the **same side panel** as edit (Snippets-style); inline add form removed.
- **Snippets:** edit and **New Snippet** use a **right slide-over panel** with backdrop (same pattern as Dictionary) instead of the separate `snippet-detail` full-page route.
- **Updater signing guardrails:** tagged releases fail fast if `TAURI_SIGNING_PRIVATE_KEY` is missing; workflow logs the trusted minisign public key id from `tauri.conf.json`. `./tasks.ps1 verify-updater-signing` checks that `plugins.updater.pubkey` matches `%USERPROFILE%\.tauri\kalam.key.pub`; `./tasks.ps1 show-pubkey` now copies the **base64** string required for the JSON `pubkey` field (avoids `Minisign(UnexpectedKeyId)` when CI secrets and config disagree).

## [0.1.0-beta.3]
- **Fix:** **Docs site** mobile nav panel: stronger frosted scrim (**`color-mix`** with theme surfaces), **48px** backdrop blur, **`isolation`**, and noise layer **`z-index: 0`** with links above it so page text no longer reads clearly through the menu.

### Fixes
- **Logging not capturing entries despite enabled setting:** `app_log::init` was called with `enabled: false` (hardcoded default), discarding all logs until `reconfigure` ran in the Tauri setup closure. `init` now reads the user's config file from disk at startup so logging is active from the first log statement. Mutex poisoning in the log state no longer silently kills logging — the inner state is recovered. `log::set_logger` failures are now reported to stderr instead of silently discarded.
- **Clipboard injection pasting stale/previous text:** `PASTE_WAIT_MS` was 100ms — too short for XAML, Electron, and heavy apps to process Ctrl+V before the clipboard was restored to its previous content. Raised to 350ms with a verification loop (up to 3 extra 100ms polls) that confirms the target app consumed the paste before restoring. Clipboard content is verified immediately after setting it. If an external clipboard manager changes the clipboard after paste, restoration is skipped.
- **Logging diagnostic:** new `get_logging_state` command exposes the backend's effective logging config so the Settings UI can detect and warn when the backend state doesn't match the user's settings.

### Features
- **Public site (`docs/download.html`):** asset items matching the visitor's detected OS now show a **★ Recommended** badge and an accent border highlight, so users can quickly identify the right installer.
- **Public site (`docs/index.html`):** **App Showcase** — 3D floating scene of real Kalam screenshots at varying perspectives, depths, and angles (Product Hunt-style eye candy); **Light / Dark toggle** swaps all 8 image sets; **click-to-zoom lightbox** with blur backdrop; screenshots cropped to remove Windows 11 chrome (title bar, borders, status bar). 8 views: Overview, History, Notes, Tasks, Reminders, Snippets, Settings General, Settings Privacy.
- **Public site (`docs/index.html`):** design-system **comparison table** (Kalam vs Wispr Flow vs Superwhisper) highlighting **open source**, Linux, BYOK, and offline STT; footnotes link to vendor sites for pricing accuracy.
- **Overlay:** when **Hybrid** or **Auto** uses **sensitive app** patterns, focusing a matching app **without dictating** briefly expands the pill (**Sensitive app detected**, lock, **amber**); dictation in that context still uses the same hint and **amber** waveform.
- **History → Clear all:** more reliable SQLite clear (transaction + vec rows first); emits **`history-cleared`** so list and overview refresh; dev bridge implements the flow for browser/Cypress.
- **Local STT first-time install:** **`download_model`** downloads the **engine (sidecar) before model weights** so start/retry is less likely to need a second attempt.
- **Windows:** local STT sidecar processes use **`CREATE_NO_WINDOW`** and null stdio so a console window does not stay visible.
- **Settings → Test microphone:** **Record** / **Play** for the last capture (no forced auto-playback).
- **Settings → Audio cleanup:** preset descriptions, illustrative **frequency sketch** (`FilterPreview`), friendlier custom labels.
- **Dictionary:** edit existing terms inline (**`update_dictionary_entry`**).
- **Pre-release builds:** update channel defaults to **beta** until the user explicitly chooses a channel in About (**`update_channel_locked`**).
- **Overlay:** transcription errors stay **longer**, with **Dismiss** and **Retry** (focuses main window); **`focus_main_window`** IPC.
- **Command mode — online search:** say **“online search”** followed by your query to open the system default browser with a DuckDuckGo search (no note/task entry). Uses `tauri-plugin-opener`.
- **New MacOS Build:** added a new MacOS build to the GitHub Releases. It is a `.pkg` file that can be installed using the standard installer. It is built for the following architectures: `arm64`, `x86_64`, `universal2`.

### Fixes
- **History detail → Sensitive apps:** the toggle now reflects whether the target process is already listed (regex match aligned with the backend), removes the correct pattern when turned off, and no longer appends duplicate patterns when the app was added from Settings.
- **History → Clear all:** the confirmation dialog is no longer clipped or unclickable — the page intro animation now ends with **`transform: none`** so `position: fixed` overlays attach to the viewport (a lingering transform on `.page.fade-in` had pinned them to the scroll container).
- **macOS:** bundle **Info.plist** includes **`NSMicrophoneUsageDescription`** so the installed app can appear under **Microphone** privacy and receive the system prompt (was missing in beta builds).
- **History row meta:** action chips wrap and respect narrow layouts (**`.entry-actions`** / **`.item-meta-row`**).
- **Snippets:** expansion matching is **case-insensitive** (Rust + snippet detail route).
- **Settings header:** **Saving…** reserves space so the layout does not jump.
- **Status bar / history detail / snippets:** more **consumer-friendly** labels (e.g. Storage, response time, transcription wording).

### Changes
- **Terms of Service (`docs/terms.html`):** comprehensive liability hardening — blanket damage exclusion replacing the $10 floor, explicit exclusion of indirect/consequential/punitive damages, new indemnification clause, assumption of risk, "not for critical use" disclaimer, third-party STT provider liability exclusion, class action waiver, pre-release software disclaimer, contributor non-infringement warranty, and severability clause. Sections renumbered (now §1–§18).
- **Privacy Policy (`docs/privacy.html`):** clarified data controller designation (user is controller for local data), added explicit third-party STT provider data handling disclaimer with links to Groq/OpenAI privacy policies, added "no absolute security guarantee" qualifier, new international data transfers section, new cookies/tracking disclosure, and new privacy-specific limitation of liability section. Sections renumbered (now §1–§14).
- **Public site (`docs/download.html`):** **All downloads** heading row always shows **Stable** and **Beta** with tag names or **Not available, coming soon**; when both channels exist, **Stable release** / **Beta release** eyebrows separate the grids. Intro line (`#release-tag-line`) matches the same wording.
- **Public site (`docs/download.html`):** **Recommended** asset badge uses a gold gradient, light rotation, and lifted shadow so it reads slightly “off the card.”
- **Public site (`docs/download.html`):** deep links to an OS row (`#downloads-windows` / `#downloads-macos` / `#downloads-linux`) skip timed auto-download, same as `?noautodl=1` — landing hero OS icons keep using the query param on those URLs.
- **Public site (`docs/documentation.html`):** full redesign of the documentation page — editorial hero with version badge, grouped sidebar navigation with icons and scroll-tracking active state, OS-tabbed installation guides, numbered quick-start steps, STT mode comparison table, hotkey reference tables, workspace feature cards, callout system (tip/warning/info variants), screenshot placeholders with descriptive labels for 14+ screenshots, keyboard shortcut styling, code blocks with language labels, prev/next bottom navigation, and responsive mobile layout (sidebar collapses to pill-style tabs). Content expanded with **Overview Dashboard**, **Dictation History**, **Command Mode** (basic + LLM-powered), **Dictionary**, API key setup guide, audio filter troubleshooting, voice command reference, and sensitive app picker documentation. All hotkey defaults, STT mode descriptions, settings paths, and feature details verified against the codebase.
- **Public site (`docs/documentation.html`):** hero release line uses the **GitHub Releases API** (same stable vs prerelease rules as `download.html`): **separate pills** for **Stable** and **Beta** when both exist; otherwise a single **Stable** or **Beta** pill. If the request fails or returns no releases, **`data-fallback-stable`** / **`data-fallback-beta`** on `#docs-hero-versions` supply text; if those are empty, a link to GitHub releases is shown.
- **Public site (`docs/index.html`):** App showcase screenshots **follow the site theme** (`data-theme`) via the **header** theme toggle only; **`setShowcaseTheme`** runs from **`setTheme`** and an **inline sync** after the grid (no duplicate Light/Dark controls in the showcase).
- **Public site (`docs/download.html`):** **Before you install** block is a clear lede card; **Permissions** and **Security prompts** are separated with eyebrow headings; permission rows use glass **cards**; OS-specific help uses design-system **accordions** (replacing ad-hoc toggles). **`showGuide`** only opens the visible OS guide; GitHub error path shows the **unknown OS** guide only.
- **Public site (`docs/index.html`):** Review Wall uses three columns with **opposing vertical marquees**, **top/bottom fade masks** (transparent → opaque → transparent), staggered durations, and **hover to pause**; respects **`prefers-reduced-motion`**.
- **Public site rebuild:** full rebuild of kalam.stream from the design system. Renamed old `index.html` → `design-system.html`; extracted shared tokens/components into `theme.css`. New pages: **landing** (`index.html` — hero, features, 3 video demos, 3D app showcase, comparison table, review wall, FAQ, pricing), **download** (`download.html` — OS detection, GitHub Releases API auto-download, security guides), **documentation** (`documentation.html` — skeleton with getting-started, STT modes, hotkeys, workspace, troubleshooting), **commercial licensing** (`business.html` — license tiers, contact, sponsors), **privacy policy** (`privacy.html`), **terms of service** (`terms.html`). Added `CNAME`, `robots.txt`, `sitemap.xml`. All pages share the design system: Bricolage Grotesque + Manrope, teal accent, dark/light theme toggle, glass nav, consistent footer.
- **Overview dashboard:** reworked charts to be value-first, with a new hero chart that combines 7-day words and estimated time saved vs 40 WPM typing (single chart, dual-series), plus clearer value-oriented labels across all overview tiles.
- **Permissions UX (macOS/Windows/Linux):** onboarding and Settings now show explicit capability status (microphone, text insertion, global hotkey), OS-specific guidance, and clearer remediation actions instead of one-size-fits-all prompts.
- **Cloud STT prompt leakage:** dictation now strips leading dictionary/prompt echoes per chunk and chains only sanitized text, preventing custom dictionary keywords from being injected at the start of transcripts.

## [0.1.0-beta.1]

### Features
- **Uninstall / distribution:** Windows NSIS **Delete the application data** now also removes **`%USERPROFILE%\.kalam`** and **`%LOCALAPPDATA%\Kalam`** (custom hooks); Debian **purge** runs **`scripts/postrm.sh`** for the sudo user’s **`~/.kalam`** and **`~/.local/share/kalam`**; **GitHub Actions** builds a **`.pkg`** per macOS arch and uploads it beside **`.dmg`**. **Website** auto-download prefers **`.pkg`** on macOS when present. **README** and **documentation** describe uninstall paths and **Settings → About** updates.
- **Settings → Local Model:** while a model is **Running**, show **approximate RAM** (sidecar RSS); **`get_model_status`** includes **`rss_bytes`** (updated on the existing ~2s poll).
- **History detail** → **Target app:** **Sensitive** toggle stays in sync with **Settings → Sensitive apps** via **`settings_updated`**.
- **Settings → Privacy → Sensitive apps:** redesigned with a user-friendly **app picker** — users can add sensitive apps from **currently running processes**, **installed applications**, or by **browsing for an executable file**. The app stores regex patterns internally, but users no longer need to write them manually.
- New Rust commands `get_running_apps`, `get_installed_apps`, and `pick_executable_file` to support the sensitive apps picker with icons and display names.
- **History retention** (Settings → Privacy) is enforced: entries older than the selected period are removed on startup and after each new dictation save (`0` = forever). The existing **1000-row** cap still applies.
- **Settings → Privacy → Notifications:** toggles for **Play sounds**, **Show error notifications**, **Show completion notifications**, and **Notify when updates are available**; Rust now respects **`sound_enabled`**, **`show_errors`**, **`show_completion`**, and existing **`show_updates`** behavior.
- **Settings → Speech-to-Text Mode** includes **Auto** (same as status bar); cloud and local sub-sections show for **Auto** so the mode matches the rest of the UI.
- **Launch at Login** now works — toggling the setting in **Settings → Startup & Behavior** registers/unregisters the app with the OS login system (Windows registry, macOS LaunchAgent, Linux XDG autostart) via `tauri-plugin-autostart`. Skipped in dev builds.
- Optional **audio filter** pipeline (high-pass, noise gate, compressor, peak normalize) before speech recognition; **Settings → Audio & Dictation** uses one **Audio filter** control: **Off** / **Light** / **Moderate** / **Custom** (sliders when Custom); **Test Microphone** playback matches filtered audio. Same processed buffer is reused across transcription **retries** (no change to retry logic).
- **Tasks** reorder: **pointer-based** drag from the **whole row** (checkbox and **row actions** stay click-only; same pattern as Notes — no HTML5 DnD), **floating row** with **`.page.fade-in` offset fix**, **`elementFromPoint`** drop resolution, **drop spacers**, **`flip`** animation; order persisted as **`note_order`**; DB lists tasks by **`note_order ASC`**. Sort cycle includes **Custom order** (manual). Reorder when not in **Trash** and with **no search / tag filter** (same idea as Notes).
- **Status bar** is **runtime-first**: mirrors dictation phase from the overlay via **`overlay-state-broadcast`**, shows **recording timer** and **input level bars**, **latency** always visible (**⚡ —** until a transcription reports ms), **Database** label + **STT mode menu** (Cloud / Hybrid / Local / Auto) with **▼/▲** chevrons (aligned with mic `<select>` affordance), **language badge** when multiple languages are set, **Cancel** while processing, **microphone** `<select>` (same `audio_device` as Settings), **plain-text hotkeys** (no kbd pill, **400** weight), **no dot separators** (flex **gap**), **emoji-only** chrome (no icon font), **primary click** toggles dictation (`ui_toggle_dictation`), **DB** retries when disconnected.
- **Notes** (active list, no search / label filter): **drag the card** (title/body area — not the footer actions) to **reorder** within **Pinned** and **Others**; drop on the other section or **Drop here to pin / unpin** to **pin** or **unpin**. Order is stored in SQLite as **`note_order`** per pin group; sort cycle adds **Custom order** (manual) when you rearrange. DB migration adds `entries.note_order`.
- **Notes** reorder: **drop indicator spacers** — a **pulsing blue bar** appears between cards to show exactly where the dragged note will land; cards **move apart** to make space for the drop.
- **Notes** reorder: **floating drag animation** — the dragged note now **follows the mouse cursor** with `position: fixed` and real-time position updates; features **lifted shadow**, **slight scale/rotation**, and **instant feedback** (no transition lag while dragging).
- **Notes** scope popover (**Notes** / **Archive** / **Trash**) shows **total counts** per list (from DB `get_note_scope_counts`); counts refresh when the list loads and when the menu opens.
- History page: **Clear all** opens an **in-app confirmation** (alert dialog with Cancel / Clear all), then removes every dictation via `clear_history` — avoids native `window.confirm`, which is unreliable in embedded WebViews; dev bridge and Cypress mocks handle the invoke.
- History **target app** shows a **friendly display name** and **app icon** when available: new `applications` cache table and `target_app_name` on entries; IPC `get_app_icon` / `resolve_target_app`; Windows uses version info + shell icons, macOS reads `.app` `Info.plist` + file icons, Linux uses `.desktop` entries + freedesktop icon lookup (with GTK fallback).
- **Tiptap** rich-text editing for **notes** and **task** descriptions: formatting toolbar on selection, **`/` commands** (headings, bold/italic/underline, lists, task checkboxes, highlight, code, quote, divider, link), **bubble menu** for quick marks; legacy plain text / markdown is loaded via `marked`, new content is stored as **HTML**.
- Notes list cards show a **content preview** (truncated plain text, smaller secondary type) under the title when body text exists.
- Onboarding email step: **Continue** saves `user_email`, notification opt-in, and a one-time OS snapshot (`onboarding_os_name`, `onboarding_os_version` via new `get_os_release_info` / `read_os_release_info`).
- Onboarding Access step: microphone input picker (refresh + device list matching Settings) so the quick mic check uses the chosen device; `audio_device` saves on change and with the rest of onboarding.
- `recording_mode` in config again controls which dictation hotkeys are registered: **Hold** (hold only), **Toggle** (toggle only), **Both** (default when unset)—without clearing stored hotkey values.
- Reminders page toolbar matches Notes/Tasks: search field with embedded icon, source tabs (All / Reminders / Notes / Tasks), tag chips when any entry has labels, and a sort cycle whose default is **Grouped** (sections: Recurring, Due today, Upcoming, Past) plus soonest/latest/title sorts.
- Tasks: **Trash** tab, soft-delete from the list or detail, **restore** / **delete permanently** in Trash, **Empty trash** (tasks only); backend `get_entries_by_type` for **task** respects **active** vs **trash** like notes. New IPC: `empty_task_trash`.
- Marketing site `download.html`: auto-starts the correct GitHub release asset for the visitor’s OS, OS-specific security prompt guides (SmartScreen / Gatekeeper / Linux), permissions heads-up, and an “All downloads” list (latest release assets) open by default.
- Settings → About → Updates: **Update now** (download, install, restart immediately) and **Update on next start** (install without restart; message confirms it applies next launch).
- Transcription handling: dynamic timeout from today's average latency (tiered cloud vs local, min/max clamp), retry up to 3 attempts with exponential backoff for retriable cloud errors (429, 5xx, network).
- Overlay processing UX: "Transcribing" after ~3s, "Slow response..." from half of expected time (min 8s) with elapsed counter; immediate "Retry N/3" on retry start; cancel button (hover or after 5s); success checkmark delayed up to 1.5s after a retry so the retry badge stays visible.
- Prototype Notes: Full notes flow aligned with main app. Notes list has scope tabs (Notes / Archive / Trash), search bar, label filters; cards show title, content preview, tags, reminder, updated date; drag-and-drop reorder (active scope); click card or "New Note" opens full-page note detail. Note detail: edit title, content, color palette, reminder datetime, pin, labels (add/remove/suggestions); Save, Cancel, Delete; create and update mock notes.
- Prototype UI: Completely reimagined with a sleek, minimalist, Apple/OpenAI-inspired aesthetic. Features: Inter typography; high-contrast monochrome color scheme (pure white/black backgrounds, subtle gray elevated surfaces); removed all heavy borders in favor of soft shadows and background tints; weightless sidebar; prominent pill-shaped dictation button with pulse animation; clean, airy layout with generous whitespace; refined timeline and cards.
- Empty state redesigned with animated visual illustration: pulsing microphone rings, sound wave bars, gradient background, and clearer CTA with styled hotkey.
- Settings sections are now collapsible (starting with General tab): click section header to expand/collapse; advanced sections collapsed by default.
- Waveform animations enhanced with GSAP: entry/exit transitions (scale + fade), command mode crossfade between blue/pink gradients, Waves waveform has parallax layer animation. Bars waveform uses CSS transitions instead of GSAP to prevent flickering from constant data updates.
- Task detail panel: wider side panel (520px), markdown toolbar (bold, italic, list, link) with Edit/Preview tabs, priority field (None/Low/Medium/High) with list badges, subtask reorder (move up/down), lighter placeholders, due/reminder layout tweaks, Save and Cancel side-by-side in footer.
- Browser dev: when the app is opened in a normal browser (e.g. http://localhost:5173/), onboarding is skipped and a default config is used so the main UI can be debugged without Tauri. In Tauri, `?skipOnboarding=1` forces skipping onboarding for testing.
- Windows hotkey low-latency: default hotkey path is the `WH_KEYBOARD_LL` hook (same thread as key delivery); set `KALAM_USE_RDEV=1` to use rdev (~500 ms gap accepted). Findings and env vars documented in `.doc/latency-profiling-windows.md`.
- Latency test: optional willhook-based hotkey listener. When `KALAM_USE_WILLHOOK=1` (Windows), the app uses the willhook crate instead of rdev for global keyboard listening to compare OS_key_down→T0 latency (rdev showed ~500 ms gap). Rdev remains the default; see `.doc/latency-profiling-windows.md` and inline comments in `hotkey.rs`.
- Latency debug "before T0" test: when `KALAM_LATENCY_DEBUG=1`, a Windows `WH_KEYBOARD_LL` hook logs each key-down as `OS_key_down_0x{VK}` to `~/.kalam/latency-trace.log`. Compare that timestamp to T0 (rdev callback) to confirm whether delay is before our process (key → OS) or inside rdev.
- Latency debugging: when `KALAM_LATENCY_DEBUG=1` (or `true`), a unified trace is written to `~/.kalam/latency-trace.log` with microsecond timestamps. Rust trace points T0–T5 (hotkey callback, spawn, resize, emit, play_sound, nudge) and frontend T6–T9 (script load, overlay-state received, Svelte tick, requestAnimationFrame) plus a `trace_latency` Tauri command so the overlay can report JS-side timestamps for correlation.

### Fixes
- **Release / CI:** `svelte-check` failed because `tsconfig.json` lacked `$lib` path aliases (Vite had them only at build time); added `paths`, `vite/client`, strict TS fixes (Prototype, Home Apex options, App overlay listener cleanup, Tiptap toolbar reactivity, etc.), and Rust **clippy -D warnings** cleanups so tag builds complete.
- **History detail** **Copy** (header and transcript): shows the same **Copied** feedback as the **History** list (label + check icon, short timeout).
- **Settings → Local Model** list: **Start / Stop / Delete** sit on the **same row** as the model title and metadata (not stacked below).
- **Language toggle hotkey** now emits **`settings_updated`** after swapping languages so the **status bar** language badge and **`sidebarDictationStore`** stay in sync (previously only the overlay toast updated).
- **Settings** (dark mode): **section accordion** carets and **tab strip** icons could stay **system-dark** on native `<button>`s (WebView), so chevrons looked missing or “stuck”; headers now set **sleek `color`**, with explicit **SVG** sizing/`color` for carets, **tab icons**, and **horizontal scroll** arrows.
- **Settings** horizontal tab **scroll arrow** buttons: hover used `:root` **--bg-input-hover** (light gray) while dark mode text stayed light, so the chevron disappeared; hover now uses sleek **--bg-hover** and **--text** like the tab row.
- **Dictation truncation on quiet microphones:** long dictations could return only a few words because the VAD `speech_threshold` was `0.5` (intended for Silero probability scores, not raw sample amplitude). Lowered to `0.02` (Balanced), `0.03` (Fast), `0.01` (Accurate) — appropriate for raw f32 amplitude where normal speech sits at 0.01–0.10. Audio filter default changed from **Off** to **Light** so peak normalization runs before VAD, making the threshold consistent across all mic gain levels. Existing users who explicitly chose Off keep their setting; new installs get Light.
- **Sensitive app picker:** overlay is **portaled to `document.body`** so it is not trapped by `.page.fade-in` `transform` (scrolling the app behind the modal was possible); **`html.sensitive-app-picker-open .page-content`** gets **`overflow: hidden`**; panel uses **`kalam-sleek` theme tokens**, **`border-radius: var(--radius-lg)`**, and **full viewport height** matching the shell.
- **Onboarding** API key persistence: now saves the API key when leaving step 4 (Engine) so the key is preserved even if the user doesn't finish onboarding.
- **Onboarding** API key UI: replaced confusing "Get a key" link with a proper **Validate** button and moved the "Get your API key from..." link below as descriptive text (matching the Settings pattern).
- **Settings** horizontal **tab row**: **soft edge fades** and **left/right scroll buttons** when tabs overflow — the app hides scrollbars globally, so users often did not discover more tabs off-screen.
- **Reset application** (Settings → Advanced) now emits **`settings_updated`**, resets the **overlay** state, and **re-registers hotkeys** from the fresh default config so the shell matches disk without restarting (previously only **`app_reset`** ran and global hotkeys could stay on old bindings).
- **Settings → Theme** segments: **active** state now updates immediately — `config` is reassigned on change so Svelte invalidates the row (nested `config.theme_preference = …` did not).
- **Tasks** list reorder: resolve **drop before** clearing drag state (reliable `elementFromPoint`), remove bad **empty-section** early return, **`update_entry` in dev bridge** for Vite + bridge, and **fail persist** if `update_entry` returns false — fixes order snapping back after drop.
- **Note** and **task** detail: **Save** and header-adjacent controls now track **Tiptap** and picker edits — Svelte 4 does not re-run templates on nested `draft.foo = …` assignments, so **note** **Save** could stay **disabled** (body text but no title) or UI could lag; updates now use `draft = { ...draft, … }`. **Note** save surfaces **load/API errors** instead of failing silently; **task** save no longer **exits without persisting** when the entry is missing, and shows **save errors** in-page. **Note** header **pin** toggle uses the same **draft** reassignment pattern.
- **Snippet** detail: **trigger/expansion** use **top-level** `bind:value` (avoids Svelte 4 stale **Save** disabled state), **loading** state while resolving the snippet, **save/delete** errors shown in-page, and **delete** asks for **confirmation**.
- Main shell **sidebar collapse** control did nothing — **`.sidebar-content`** (full-width, `z-index: 1`) sat above the **`.sidebar-toggle`** strip and ate pointer events; toggle is now stacked above that strip only.
- **Tasks** list at narrow widths: removed container rules that **wrapped** the row and forced **`.task-row-actions`** onto a full-width second line (extra row height). Rows stay **single-line** with **ellipsis** on the title and **horizontal scroll** for tag pills when needed.
- **SveltyPicker** display for 12-hour locales used the wrong hour token (`hh` is 24h in svelty-picker’s standard format), showing values like `14:10 pm`; now uses `HH` for 12-hour clock and `P` for uppercase AM/PM.
- **Notes** reorder: fixed **mouse vs floating card offset** — the `.page.fade-in` ancestor has `animation: fadeInPage` which leaves a `transform: translateY(0)` via `forwards` fill; any ancestor with a CSS `transform` makes `position: fixed` relative to **that element** instead of the viewport. Now measures the ancestor's viewport rect at pointer-down and subtracts it from `left`/`top`. Also uses the **article's** `getBoundingClientRect` (not the wrapper) and captured **width/height** instead of a hardcoded value.
- **Notes** reorder: **pointer-based** move (press on note body, drag, release) — **HTML5 drag-and-drop** is unreliable in **Tauri / WebView2**; no grab cursor; lifted card uses **`pointer-events: none`** so the drop target under the cursor resolves correctly.
- **Notes** reorder: explicitly set **`pointer-events: none`** on the dragged element before calling `elementFromPoint()` during drop resolution. Svelte's async class binding caused the hit test to see the lifted card instead of the drop target beneath it.
- **Sort** on Notes / Tasks / Reminders: **`on:click|stopPropagation`** + **`cycleSortMode`** closing popovers; list order uses a **`$:` block** with **`void sortMode`** (comma-operator / comparator-only deps did **not** reliably re-run lists when sort changed — button title updated but cards stayed put).
- **Notes** / **Tasks** / **Reminders** toolbars: **outside** dismiss for popovers used **`pointerdown`** on `document`, which ran **before** the **sort** (and other) toolbar buttons received **`click`** — closing popovers and re-rendering could **cancel** the control. Dismiss now uses **`click`** so toolbar actions run first.
- **Notes** list: **`scopeCountFor` / `loadScopeCounts` were referenced but never defined** (runtime error broke the whole view after adding scope counts).
- **Notes** / **Tasks** toolbar at **narrow** `list-toolbar` widths: the sort strip still used `flex: 1 1 100%` from when scope lived *inside* the scroll row — the scope sibling **collapsed to zero width** (broken clicks). Sort strip is **content-sized** again; scope dropdown gets **`min-width: 48px`** so it cannot disappear.
- **Notes** / **Tasks** scope popover was **invisible** — the toolbar’s horizontal scroll strip uses `overflow-x: auto`, which clips `position: absolute` dropdowns; **scope + popover** moved **outside** that strip (sort stays inside).
- **Reminders** list stuck on “Loading reminders…” — `filteredEntries` still referenced removed `selectedTag` (runtime error); restored **multi-tag OR** filter using `selectedTags`.
- Opening a **note** or **task** from **Reminders** (list row) returns to **Reminders** on back/cancel; opening detail from **Notes**, **Tasks**, or **Home** clears that context so back still goes to the list you opened from.
- Snippets list: hide **search** when there are no snippets yet; **empty / no-results** message spans the grid so icon and copy are centered horizontally.
- Notes list layout uses a **responsive CSS grid** with a **max card width** and **no fixed square aspect ratio**, so cards read larger on wide windows and no longer balloon to huge squares near minimum app width (replaces column-count masonry + `aspect-ratio: 1/1`).
- Tasks and Reminders scope tabs: long labels (**Open** / **Completed** / **Reminders**, etc.) no longer bleed into adjacent tabs — each tab clips with **ellipsis** (`overflow: hidden` on the segment and label). Previously ellipsis only applied under the 960px viewport rule, so mid-width layouts could overlap text.
- Notes / Tasks / Reminders lists: shared toolbar — **scope + sort** (and Tasks **tag filter**) stay on one row; tab labels icon-only at ≤480px (`list-toolbar`); **tag chips** (Notes + Reminders) scroll horizontally at ≤480px (`sleek-page`); tighter search padding; **actions row** can scroll horizontally when needed (e.g. four tabs + controls); slightly tighter segment track at ≤380px. Viewport ≤960px no longer forces scope full-width beside sort.
- Microphone select: **System default** and the default device row no longer share `value=""` (duplicate values broke HTML selects); device rows use real ids (`default`, `device_0`, …). Settings load no longer collapses stored `default` to empty. Onboarding keeps reactive save + init guard.
- Reminders SQL `ORDER BY`: SQLite `COALESCE()` requires at least two arguments; removed invalid single-argument wrapper around the sort expression.
- Reminders view lists open tasks that have a **due date** or **reminder** time (not only standalone reminders and notes); backend `get_entries_with_reminder` matches the combined prototype list, and the row shows the correct time via `reminder_at` or `due_date`. Recurring rows use the **arrows-clockwise** icon whenever `rrule` is set (plus the existing “Recurring” chip).
- Task detail subtasks: drag-and-drop reorder (like the prototype) and drag-handle chip styling (border and background on the grip so it reads clearly on each subtask row).
- Onboarding “Ready” demo textarea uses `bind:value` so dictation updates the field (Svelte 4 `value=` alone does not update after mount).
- Hotkey capture uses `.kalam-sleek` tokens (capture surface and key pills) for consistent light/dark styling with Settings and onboarding.
- Hotkey UI shows the correct OS/meta key label: Win (Windows), Cmd (macOS), Super (Linux); stored spellings (Win/Super/Cmd/Command/Meta) normalize for display. Onboarding uses `macos` from `get_platform` (Rust `std::env::consts::OS`) instead of `darwin`.
- Overlay collapsed hint and onboarding toggle hotkey use the same platform-specific meta display as the rest of the UI; `settings_updated` refreshes `sidebarDictationStore` so the status bar hotkey text updates after saves.
- Pill shadow reduced to a very subtle 1-2px depth that smooths the corners/borders without creating a prominent glow effect. Removed all colored glows and large drop shadows from collapsed, expanded, recording, success, and error states.
- Removed idle breathing/pulsating animation from the dormant state pill. The pill now maintains a steady opacity of 0.7 in collapsed state instead of pulsing between 0.5 and 0.8.
- Overlay pill flicker on hover/expand: integrated GSAP animation library for smooth, GPU-accelerated transforms. The pill uses `scaleX`/`scaleY` transforms instead of width/height animations; content counter-scales to maintain text size and fades in/out with synchronized animations. Added initialization delay to prevent initial mount flicker.
- Overlay pill shadow no longer clipped: overlay window size includes 24px margin so pill box-shadow can spill; `.blip-root` set to `overflow: visible` so shadow is not cut off.
- Hold/command hotkey "didn't hold long enough" false positive: press time is now recorded in the sync key-down callback (rdev thread) and copied into app state at the start of the async task, so hold duration is measured from actual key press, not from when the async task ran. Previously a delayed async task could make a long hold appear short and trigger cancel.
- Overlay pill delay (~1 s): pre-resize/collapse the overlay window from Rust before emitting state events, eliminating the JS→Rust IPC roundtrip that required the unfocused WebView2 to process a resize command. Also add a Web Worker keepalive to prevent WebView2 from throttling the overlay's JS event loop when unfocused, and enable `--disable-background-timer-throttling` browser arg for both windows.
- Start-of-dictation latency: hotkey press/release/cancel now run on Tauri's async runtime instead of a dedicated hotkey runtime, avoiding cross-runtime lock contention that could add ~1 s delay.
- Foreground process name is resolved in a background task after storing the HWND (aligned with v0.0.1-beta.5 behavior) so the start path isn't blocked by OpenProcess/QueryFullProcessImageName.
- Start-of-dictation latency: show overlay pill and start sound immediately, then position overlay and capture foreground window, so heavy work no longer blocks visible/audible feedback.
- Start sound no longer blocks the hot path: WAV read and playback run in spawn_blocking + thread so the dictation flow isn't delayed by disk or audio init.
- Dictation cloud API keys are now stored per STT provider, so switching between Groq/OpenAI uses the correct key and Settings shows provider-specific configured state.
- Hold/command hotkey handling now guards startup with a dedicated `Starting` audio state to prevent short-press cancel races that could leave dictation in inconsistent states.
- Dictation stop latency reduced by replacing fixed 300ms recorder shutdown sleeps with a bounded adaptive drain.
- In-app log export: use native Save dialog so log/CSV files are actually saved (blob download does not work in Tauri webview); refresh log-empty state on Settings load; show clear feedback when there are no entries or save fails.
- Prevent multiple instances; re-opening the app focuses the existing window (Issue #9).
- Overlay pill in dormant state: collapsed window size increased to 52×14 so pill border is not clipped; hover expansion and "Press … to dictate" text delayed 60ms so overlay window resizes before content shows; removed border-radius from overlay root so the pill is not clipped by the root's capsule shape on hover (fixes cut-off edges, #7).

### Changes
- **Rust:** ran **`cargo fmt`** on `src-tauri` so **`cargo fmt -- --check`** matches the formatter on **GitHub Actions** `stable` (workflow was failing the format step after checks passed).
- **Settings → General → Dictation Hotkeys:** accordion open/close uses a **short slide** animation (~180ms) as a first step toward smoother section toggles.
- **Settings → Speech-to-Text Mode:** mode cards order is **Auto → Hybrid → Cloud → Local** (left to right).
- **History detail** stats layout: **metrics** (words, optional wpm, length, latency) in one **equal-cell** row; **Recognition** and **Language** in a **two-column** row; **Target app** (with **Sensitive apps** toggle) on its own **full-width** row — avoids a lone **Recognition** tile and uneven **Language** / **Target** heights.
- **History:** saved **language** on **Home → Recent** (chip) and in **history detail** beside **Recognition**; the main **History** list has no language chip.
- **Settings → About** uses the same **collapsible section** pattern as other tabs (**section header** + caret, shared **section** spacing); **Community & support** no longer uses a separate icon bar/divider, and **Commercial license** is a **setting row** inside that section.
- **About page redesign:** Combined **Community** and **Support Kalam** into a single **Community & Support** section; moved **Terms & Conditions** and **Privacy Policy** to a new expandable **Legal** section; moved **Commercial License** to a full-width button; renamed section headings to **License** and **Legal** (dropped "Information"); reorganized updates section with **Current version** and **Check now** on the same row and **Channel** below; updated styling to match **Data & Privacy** section (same padding and row structure); updated creator credit from "Created by Ali Farahat, founder of…" to "Brought to you by Ali Farahat at…"; heart icon now properly respects light/dark theme.
- **Settings → Speech-to-Text Mode:** **Cloud Provider** and **API key** (including the “Get your API key…” link) sit in one **`.stt-cloud-group`** — single left accent, no horizontal dividers between those rows.
- **Settings → Speech-to-Text Mode → API key:** shows **✓ Configured** / **Not set** badges (same idea as Command Mode) so it is obvious when a key exists without revealing it.
- **Settings → Speech-to-Text Mode:** mode choices are a **single horizontal row** (icon + label) with the **full description** shown below for the selected mode.
- **Sensitive app picker:** full-viewport sheet; **body scroll locked** while open; **only the app list scrolls**; tab bar is a **fixed-height** three-column strip; **Running** / **Installed** use a **list** (not a grid); **Installed** list loads only after **Load installed apps** (avoids long hangs on first open); default password-manager regex includes **NordPass**.
- **`PrivacyAction`** is **`ForceLocal` only** in the schema; legacy **`Block`** / **`RequireConfirmation`** values in `config.json` deserialize as **`ForceLocal`**.
- **Settings → Appearance:** **Theme** uses **Auto / Dark / Light** segmented control (same options as the sidebar theme button), replacing the old **Dark mode** on/off switch.
- **Tasks** list: **no drag handle** — **whole row** pointer drag-to-reorder again (threshold distinguishes click vs drag); **pointer** cursor kept (not grab/grabbing). Subtasks on task detail still use a separate handle.
- **Note** detail: **reminder** is an inline field below labels (no bell popover), using the same **`reminder-section` / `section-title` / `due-date-input-row`** layout and globals as **Task** detail; **color** and **pin** moved to the **header** next to archive / delete / cancel / save; color popover opens **below** the swatch in the header.
- **Notes** list: the **Pinned** section (heading and empty **Drop here to pin** strip) is hidden when there are no pinned notes; while **drag-reordering**, the pin drop target still appears so the first note can be pinned. The **Others** heading is shown only when **Pinned** is present so an all-unpinned list reads as a single grid.
- **SveltyPicker** text field uses **14px** like other sleek detail inputs (subtasks, labels); `app.css` had forced **12px** on the inner input and overrode `App.svelte`.
- **Tasks** default scope is **All** (was Open); **All** lists **Open** then **Closed** section headings (incomplete vs completed); scope menu order: **All**, Open, Completed, Trash; **Open** / **Closed** spacing uses **`--space-2xl`** between sections (same as Notes **Pinned** / **Others**), with **`task-list-section-rows`** keeping **8px** between rows inside a section.
- List toolbar (**Notes** / **Tasks** / **Reminders**): **actions** cluster stays **right-aligned** — `margin-left: auto` when search is hidden, and **scope** no longer uses `flex-grow` (it was stretching the scope cell left on full-width toolbars); scope **trigger** and **active** popover row use **medium** weight (`500`), not semibold.
- **Notes** and **Tasks** list scope (**Notes** / **Archive** / **Trash** and task scopes) is a **single toolbar control** opening a **popover** matching the **label filter** layout (heading + scrollable list with icons); removed inline **segmented** tabs.
- **History** list toolbar matches Notes / Tasks / Reminders: **`notes-toolbar`** with **`notes-search-bar`** + **`notes-sort-cycle`** (newest vs oldest by time); same flex alignment as other list pages.
- **Tasks** tag filter matches Notes / Reminders: **multi-select** tags with **search**, **checkbox OR** filter, **Clear all**, **count badge**, shared **popover** styling; empty state when **filters match nothing**.
- Notes / Tasks / Reminders toolbar: **search bar** uses remaining width (`flex: 1 1 0`); **actions** column is **content-sized** (`flex: 0 0 auto`); **one row** at default/min window width — removed **960px** column stack; **container** full-width stacking only when toolbar is under **520px** wide; **560px** viewport fallback for stacked toolbar.
- Notes / Reminders **label tag filter** popover: **scroll strip** + sibling trigger avoids **clipping**; **title** (Labels / Tags) + **Clear all** + search; **sleek** surfaces and **inset search**; **radius/shadow/border** aligned with scope menus; tighter checkbox list.
- **Reminders** tag filter: same **sort-style** tag button in the toolbar as **Notes** — popover with **search**, multi-select **checkboxes** (OR), **Clear all**, **count badge**; click outside closes. Inline tag chips removed.
- **Notes** label filter: **sort-style** tag button (next to sort) opens a **popover** under it with **label search**, checkboxes (OR filter), **Clear all filters**, and a **badge count** of active filters; click outside closes.
- Reminders page: removed **New note** / **New task** header actions (create from **Notes** / **Tasks**; subtitle still says so).
- Deleting a **task** or **note** (detail view or list **trash** / **move to trash**) asks for **browser confirmation** before proceeding; note detail already confirmed—list actions and task detail now match.
- Task detail: **priority** stays compact in the title row but opens a **custom popover** (flags, full labels, checkmarks) instead of a native select; **complete** control is larger with **`ph:circle-bold`** when incomplete and a **ring border** so it does not read as a tiny stray circle.
- Date and time fields use **svelty-picker** (`^5.2.12`, Svelte 4–compatible) instead of native `datetime-local` inputs in note reminder popover, task due/reminder rows, and the prototype reminder editor; **stored values stay `yyyy-mm-ddThh:ii` (24h)** for the API while **display** follows the OS locale (`Intl` + `displayFormat`), **week start** uses `Intl.Locale` `weekInfo` when available, and **month/day labels** come from `Intl`. The popup is **more compact** (narrower panel, shorter month grid, smaller time clock via `--sdt-clock-size`). Popup colors follow `.kalam-sleek` tokens via `app.css`.
- **Reminders** are not a separate entry type anymore: only **notes** and **tasks** carry `reminder_at` / `due_date` / `rrule`. Existing `entry_type = 'reminder'` rows are migrated to **notes** on database open. Voice **"new reminder"** and LLM **reminder** classification create a **note** (with optional `reminder_at`). Reminders view lists timed notes/tasks; **New note** / **New task** replace standalone reminder creation.
- Reminders source filter tabs: **All** uses **list-bullets** (not `squares-four`, which matches sidebar Overview); **Reminders / Notes / Tasks** use the same Phosphor icons as the left nav (**bell**, **notebook**, **check-circle**) with **-fill** when that filter is active. Reminder list rows for notes use **notebook** in the type icon.
- Hotkey capture (Settings & onboarding): placeholder copy explains holding two or more keys; idle and recording hints updated. Clicks on label text, key pills, and the edit icon hit the whole control (`pointer-events` on decorative children; larger clear/cancel targets) so WebView2 behaves like the padded area; clear uses `preventDefault` for reliable clearing.
- Onboarding Shortcuts step: dictation hotkeys use the same **setting row** layout and labels as Settings → Dictation Hotkeys, with a `.page-content` wrapper so the shared hotkey capture styling applies.
- Onboarding Step 4 (Speech engine): tab-style selector matching Notes/Tasks design; feature chips with color-coded indicators; shorter, scannable copy; Hybrid mode shows info box about setting up local models from Settings after onboarding.
- Onboarding mic check: spinning refresh icon while re-listing devices; **Record sample** shows **Starting…** with a spinner until capture actually begins.
- Onboarding microphone check: shorter on-screen copy; fuller guidance in info-icon tooltips and `aria-label`s.
- Onboarding Access step: permission cards sit in a **collapsed-by-default** accordion; main content column scrolls when a step is taller than the viewport (`content-scroll` / `step-shell` flex fix).
- Onboarding Access step: one **Allow on this device** panel combines the suggested order list, permission actions, and per-permission privacy notes (replacing separate “What Kalam uses” and permission list blocks).
- Layout at narrow widths (including app min width ~845px): **container queries** on `.page.fade-in` (`sleek-page`) and `.notes-toolbar` (`list-toolbar`) stack search + scope/sort, equal-width scope tabs, visually hidden tab labels under ~480px on the toolbar container (with `title` / `aria-label`); task rows wrap tags/actions under ~720px; Reminders toolbar uses `notes-toolbar-actions`; search inputs use `min-width: 0`. Existing ≤960px rules keep stacked toolbars, single-column masonry, and wrapping headers.
- Settings page matches the prototype layout: collapsible sections (caret up/down), toggle switches, segmented recording mode (Hold / Toggle / Both), STT mode cards, API key block, privacy info callout, logging actions row, danger zone; Dictionary/Command tabs use the same patterns. About in Settings uses the prototype top bar (version + channel + check) and two-column cards. Hotkey fields use the prototype pill capture control.
- Snippets: search field uses the same embedded icon pattern as Notes; **How snippets work** is a compact collapsible row (info icon + caret), collapsed by default. Cards and editor show the real **trigger** text only (no decorative `/` prefix); copy clarifies there is no special prefix and matching is exact substring in the transcript. Snippet labels/tags removed from UI and API (`add_snippet` is trigger + expansion only).
- Reminder **repeat** (`rrule`) remains on entries that support it (e.g. notes with a reminder time); OS notifications still use the single scheduled time until repeat firing is fully implemented.
- Reminders list and editor: search bar matches prototype (inline search icon); loading/error use the same `empty-state` treatment as the prototype (spinner animates without the generic `state-container` SVG spin); reminder title placeholder matches prototype (“Reminder Title”).
- Notes list: search, scope, and sort on one toolbar row; scope is a three-tab segmented control (Notes / Archive / Trash) with icons; sort cycles via an icon button (newest → oldest → title A–Z → Z–A) with tooltip and aria-label for the active mode.
- Notes: **Empty trash** moved to the page header (same slot as New Note) with pill outline styling (`btn-danger-outline`); archive/trash cards use labeled actions (**Unarchive** / **Restore** / **Trash** / **Delete**). Note detail: **Restore** for trashed notes and a labeled **Unarchive** for archived notes (both return to Notes).
- Tasks: label filter is a **tag** menu beside sort (not a chip row); toolbar uses `notes-toolbar-actions` so scope segments, sort, and label control share a 48px-tall row; Notes uses the same wrapper for alignment.
- Tasks list: reorder drag handle uses the same bordered chip style as subtasks (visible grip on each row).
- Marketing landing (`docs/index.html`): hero overview screenshot with clipped scale; Features and Documentation CTAs use watercolor illustrations; workspace bento shows app screenshots (Notes, Tasks, Reminders, Snippets, History); marketing images use a full-screen lightbox (Enter/Space to open, Escape to close; hero excluded); Features block spacing refined (`space-y-12`/`space-y-16`, caption padding, grid gaps).
- Onboarding copy, flow, and layout: step eyebrows (“Step x of 6”), clearer section headings, mode pills with subtitles, ordered permission hints, refined “Try dictation” steps, Continue / Enter Kalam actions, and a light step transition (honors `prefers-reduced-motion`). Sidebar labels shortened (Email, Access, Engine, Shortcuts, Try it).
- First-run onboarding uses the same `.kalam-sleek` theme shell as the main app (Inter, light/dark surfaces, accent buttons) and Phosphor-style icons instead of emoji.
- `docs/download.html` visual refresh: Fraunces display headline, layered mesh/noise hero, status row with pulse → success state, OS detection chip, icon-led “While you wait” panel, three permission cards, richer disclosures and file-type badges on asset rows; respects `prefers-reduced-motion`.
- `package.json`: add `docs:dev` (static server for `docs/` on port 4321); remove redundant `tauri:dev` alias (use `npm run dev`).
- Update notifications (background check + tray “Check for Updates”) now say to use **Settings → About** to download/install, matching in-app updater UX.
- Landing download CTA points to `download.html` instead of linking straight to a single GitHub URL; label still reflects detected OS when the releases API succeeds.
- Tauri command `download_and_install_update` takes a `restart` flag; restart is skipped when the user chooses next-start install.
- Cloud STT: tighter per-clip timeout floor (`12 + min(audio, 20)/2` seconds, capped at 22s) so long recordings don't balloon the deadline; local STT floors unchanged.
- Overlay pill processing layout: cancel button is now in-flow (flex child) instead of absolutely positioned, preventing overlap with the elapsed timer; smaller font sizes and tighter spacing.
- Document macOS Input Monitoring in README and explain permissions and privacy on the onboarding Permissions step.
- UI color palette harmonized with logo: new warm amber (`--warm-amber`) and soft rose (`--soft-rose`) accents; stat cards use logo-derived colors (amber, teal, soft blue, periwinkle) instead of bright rainbow; warmer neutrals throughout.
- History timeline redesigned with calendar/agenda-style day grouping: grouped by date in bordered cards with subtle warm tint on alternating days.
- Sidebar hover states colorized: teal tints on nav items, warm amber on dictation control, creating more responsive feel.
- Settings tabs improved: active tab indicator, better hover colors, reduced font weights.
- Typography quieter: reduced font weights throughout (800→600, 700→500, 600→500); removed uppercase labels; softer animations (4px→2px lift, gentler shadows).
- Badge styling refined: removed uppercase and reduced weight; subtler borders instead of colored backgrounds.
- UI font: body and headings use Google Sans for consistency; logo text keeps Syne (unchanged).
- Hybrid sensitive-app detection now resolves the active process name via direct PID lookup on Windows instead of full `sysinfo` refresh scans.
- Settings page: tab menu (General, Dictation, etc.) is sticky so it stays at the top when scrolling.
## [0.0.1-beta.8]
- **Fix:** Release workflow: global `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24`, correct tauri-action input `assetNamePattern`, Windows build uses `--bundles nsis` only to avoid MSI prerelease version error.
- **Change:** CI workflow: global `FORCE_JAVASCRIPT_ACTIONS_TO_NODE24` for Node 24 opt-in on all jobs.
- **Change:** tasks.ps1: on Windows, `build` and `build-signed` use `--bundles nsis` so local builds succeed with prerelease versions (aligns with release workflow).

## [0.0.1-beta.5]
- **Change:** CI: Node 22, npm ci, frontend unit tests; build script embeds Windows .exe icon; AppConfig version handling improved; STT handles empty results and prompt echoes.
- **Change:** Overlay and app optimization: Overlay component, Home, Snippets, History, Notes, Reminders, Tasks, lib.rs and docs.
- **Feature:** Sidebar collapse: toggle for expanded vs icon-only view; state persisted in AppConfig.
- **Feature:** Settings: model download progress and sidecar installation status.
- **Fix:** Overlay background on macOS to prevent white flashes during initialization.
- **Fix:** Injection: force clipboard for TSF-heavy apps (e.g. Win11 Notepad).
- **Change:** Version bump to 0.0.1-beta.5.
- **Change:** Docs: footer and navigation links; business page.
- **Change:** CI: macOS runners (macos-13 for x86_64), frontend build before clippy, Ubuntu deps (libxdo-dev, pkg-config).
- **Feature:** Overlay pill resizes the OS window: idle/collapsed uses 80×80 so the rest of the screen is click-through; expanded (recording or hover) uses 300×120. Preserves hover tooltip and drag.
- **Change:** License: allow commercial use for organizations with up to 2 team members without a separate license; above 5 or resale/embedding still requires a written commercial license. Business page updated to state small-team allowance.
- **Change:** GitHub page (docs): condense top nav into "More" dropdown (Workspace, Compare, Documentation, For business); keep Features, How it Works, FAQ, GitHub in bar.
- **Fix:** macOS crash on keyboard input (onboarding email field): switch rdev to fufesou/rdev fork to avoid HIToolbox main-thread assertion (dispatch_assert_queue_fail).
- **Fix:** Overlay window on macOS showing white box: enable `macOSPrivateApi` in Tauri config/Cargo and set window background color to transparent in Rust to ensure the OS allows transparent windows without white flashes.
- **Change:** Changelog added; Cursor rule to maintain CHANGELOG.md (version-grouped, used for releases).
